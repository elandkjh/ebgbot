import { Align, AlignValue, FlexSizeSpecValue, Layout, SizeSpec } from '../types/styles';

export const defaultProperties = {
  rootLayout: Layout.Column,
  view: {
    size: {
      width: { type: 'flex', value: FlexSizeSpecValue.FillParent } as SizeSpec,
      height: { type: 'flex', value: FlexSizeSpecValue.WrapContent } as SizeSpec,
    },
  },
  box: {
    layout: Layout.Row,
    align: { vertical: AlignValue.Top, horizontal: AlignValue.Left } as Align,
  },
};
