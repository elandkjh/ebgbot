import React, { ReactElement } from 'react';
interface CarouselProps {
    children: ReactElement[];
    gap?: number;
}
export declare const Carousel: React.MemoExoticComponent<({ children, gap }: CarouselProps) => ReactElement>;
export default Carousel;
